---
description: Diagnose dotclaude install health, skill shape, hooks, source tags, and config parseability.
---

Run the `dotclaude-doctor` skill at `~/.claude/skills/dotclaude-doctor/SKILL.md`.

If available, execute:

```bash
"$DOTCLAUDE_HOME/scripts/dotclaude-doctor.sh"
```

If `DOTCLAUDE_HOME` is unset but this command is being run from a dotclaude repo,
use `./scripts/dotclaude-doctor.sh`.

$ARGUMENTS

